spring:
  application:
    name: produtosapi
  datasource:
    url: "jdbc:h2:mem:produtos"
    username: sa
    password: pass
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console